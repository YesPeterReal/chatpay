apiVersion: v1
kind: Service
metadata:
  name: chatpay-service
  namespace: default
spec:
  selector:
    app: chatpay-ledger
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: LoadBalancer